---
title: Heading
description: Used to render semantic HTML heading elements.
links:
  source: packages/chakra-ui-solid/src/components/heading
  chakra: https://chakra-ui.com/docs/components/heading
---

<Example name="heading-basic" />

## Usage

```tsx
import { Heading } from "chakra-ui-solid";
```

```tsx
<Heading>I'm a Heading</Heading>
```

## Examples

### Sizes

Use the `size` prop to change the size of the heading component.

<Example name="heading-with-sizes" />

### As another element

Use the `as` prop to render the heading as another HTML element.

<Example name="heading-with-as-prop" />

### Weights

Use the `fontWeight` prop to change the weight of the heading component.

<Example name="heading-with-weights" />

### Composition

Use the `Heading` component to compose other components.

<Example name="heading-with-composition" />

## Props

<PropsTable component="heading" />
