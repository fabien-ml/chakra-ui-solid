---
title: Spinner
description: Used to provide a visual cue that an action is processing
links:
  source: packages/chakra-ui-solid/src/components/spinner
  chakra: https://chakra-ui.com/docs/components/spinner
---

<Example name="spinner-basic" />

## Usage

```tsx
import { Spinner } from "chakra-ui-solid";
```

```tsx
<Spinner />
```

## Examples

### Sizes

Use the `size` prop to change the size of the spinner.

<Example name="spinner-with-sizes" />

### Colors

Use the `colorPalette` prop to change the color scheme of the spinner.

<Example name="spinner-with-colors" />

### Custom Color

Use the `color` prop to pass a custom color to the spinner.

<Example name="spinner-custom-color" />

### Track Color

Use the `--spinner-track-color` variable to change the color of the spinner's track.

<Example name="spinner-with-track-color" />

### Custom Speed

Use the `animationDuration` prop to change the speed of the spinner.

<Example name="spinner-with-custom-speed" />

### Thickness

Use the `borderWidth` prop to change the thickness of the spinner.

<Example name="spinner-with-custom-thickness" />

### Label

Compose the spinner with a label to provide additional context.

<Example name="spinner-with-label" />

### Overlay

Compose spinner with the `Center` component to overlay the spinner on top of another component.

<Example name="spinner-with-overlay" />

## Props

<PropsTable component="spinner" />
