---
title: Visually Hidden
description: Used to hide content visually but keep it accessible to screen readers.
links:
  source: packages/chakra-ui-solid/src/components/visually-hidden
  chakra: https://chakra-ui.com/docs/components/visually-hidden
---

<Example name="visually-hidden-basic" />

## Usage

```tsx
import { VisuallyHidden } from "chakra-ui-solid";
```

```tsx
<VisuallyHidden>Hidden content</VisuallyHidden>
```

## Examples

### Input

Using the `render` prop, you can render the `VisuallyHidden` styles onto an element of your own.

<Example name="visually-hidden-with-input" />

`render` is this library's answer to `asChild`, and it is a **function** that receives the computed
props rather than a child element — for the same reason `Stack`'s separator is a component. See
[Composition](/docs/components/concepts/composition#the-render-prop) for the whole of it.

## Props

<PropsTable component="visually-hidden" />
